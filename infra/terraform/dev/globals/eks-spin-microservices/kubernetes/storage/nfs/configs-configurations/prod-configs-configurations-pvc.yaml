---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: configs-configurations
  namespace: prod
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: spin-configs-configurations-prod
  storageClassName: nfs-csi
