---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: configs-configurations
  namespace: dev
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: spin-configs-configurations-dev
  storageClassName: nfs-csi
