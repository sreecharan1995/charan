---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: spin-shared
  namespace: prod
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 10Gi
  volumeName: spin-shared-prod
  storageClassName: nfs-csi
