---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: spin-shared
  namespace: dev
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 10Gi
  volumeName: spin-shared-dev
  storageClassName: nfs-csi
