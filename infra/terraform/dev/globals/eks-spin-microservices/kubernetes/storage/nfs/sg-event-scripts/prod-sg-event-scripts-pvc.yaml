---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: sg-event-scripts
  namespace: prod
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: spin-sg-event-scripts-prod
  storageClassName: nfs-csi
