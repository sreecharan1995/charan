---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: permission-matrix
  namespace: prod
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: spin-permission-matrix-prod
  storageClassName: nfs-csi
