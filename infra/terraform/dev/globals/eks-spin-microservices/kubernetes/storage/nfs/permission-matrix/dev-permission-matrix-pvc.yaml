---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: permission-matrix
  namespace: dev
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: spin-permission-matrix-dev
  storageClassName: nfs-csi
