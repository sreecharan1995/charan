---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: sg-event-scripts
  namespace: uat
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: spin-sg-event-scripts-uat
  storageClassName: nfs-csi
